version: '2'

services:
  db:
    image: postgres
  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"
  django:
    image: louisbertin/django:v0.0.4
    ports:
      - "8000:8000"
    depends_on:
      - db
      - adminer